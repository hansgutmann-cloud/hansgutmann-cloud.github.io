{
  "name": "invariant-homepage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A quiet, executable personal website. Native WebGPU/WebGL2, an i32-to-Wasm compiler, and small systems laboratories.",
  "scripts": {
    "dev": "node scripts/build.mjs && node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs --dist",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
